Правила ошибок:

1. Ошибки внутри Query отдавать как есть.
2. Ошибки, связанные с атрибутом модели перехватываем и пишем уточнение, какие есть валидные поля.
3. Ошибки, связанные с apply_join / apply_load не игнорируем, а отдаем вместе со списком\
   возможных полей, либо с исправлением "возможно, вы имели в виду `x`?"
4. Ошибки в репозитории выдаем как есть.
5. В фильтрах отдаем ошибки FilterError только в случае, если ошибка именно самой структуры фильтров.
   Если дело в поле SQLAlchemy - отдаем ошибку SQLAlchemy/кастомную ошибку, не связанную с фильтрами.
